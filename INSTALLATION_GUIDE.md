# Руководство по установке и настройке RobBob Launcher

## 🎯 Концепция Bootstrapper

Система bootstrapper работает в два этапа:

1. **Bootstrapper (маленький файл ~10-20 МБ)** - это лёгкий установщик, который пользователь скачивает с вашего сайта
2. **Основной лаунчер (полный архив)** - скачивается автоматически bootstrapper'ом с вашего сервера

### Почему это удобно?

- ✅ Пользователь скачивает маленький файл быстро
- ✅ Автоматические обновления - не нужно вручную скачивать новые версии
- ✅ Централизованное распространение - один сервер с актуальной версией
- ✅ Простота обновления - просто обновите файл на сервере

---

## 📦 Шаг 1: Сборка основного лаунчера

Сначала нужно собрать основной лаунчер в ZIP архив.

### 1.1. Соберите основное приложение

```bash
# В корне проекта
npm install
npm run build
```

После сборки в папке `dist/` появится файл типа `RobBob-Portable.exe`

### 1.2. Создайте ZIP архив для автоматической загрузки

Вам нужно создать ZIP архив со всеми файлами лаунчера:

**Структура архива `RobBob-App.zip`:**
```
RobBob-App.zip
├── RobBob.exe                    # Переименуйте RobBob-Portable.exe в RobBob.exe
├── resources/                    # Папка с ресурсами (если есть)
├── locales/                      # Локализации Electron (если нужны)
└── (другие файлы из dist/)
```

**Важно:** Основной EXE файл должен называться `RobBob.exe` и находиться в корне архива!

**Как создать:**
```powershell
# В PowerShell в папке dist/
# 1. Переименуйте exe файл
Rename-Item "RobBob-Portable.exe" "RobBob.exe"

# 2. Создайте ZIP архив
Compress-Archive -Path * -DestinationPath "../RobBob-App.zip"
```

---

## 🌐 Шаг 2: Настройка сервера

### 2.1. Создайте файл version.json

Создайте файл `version.json` со следующим содержимым:

```json
{
  "version": "1.0.0",
  "downloadUrl": "https://ваш-домен.com/downloads/RobBob-App.zip"
}
```

**Параметры:**
- `version` - номер версии (при изменении bootstrapper скачает обновление)
- `downloadUrl` - прямая ссылка на ZIP архив

### 2.2. Разместите файлы на сервере

Загрузите на ваш сервер:

```
https://ваш-домен.com/
├── version.json              # Информация о версии
└── downloads/
    └── RobBob-App.zip       # ZIP архив с лаунчером
```

**Примеры размещения:**

**GitHub Releases:**
```json
{
  "version": "1.0.0",
  "downloadUrl": "https://github.com/username/repo/releases/download/v1.0.0/RobBob-App.zip"
}
```

**Ваш веб-сервер:**
```json
{
  "version": "1.0.0",
  "downloadUrl": "https://robbob.example.com/downloads/RobBob-App.zip"
}
```

### 2.3. Настройте CORS (если нужно)

Если используете веб-сервер, добавьте CORS заголовки:

**Nginx:**
```nginx
location /version.json {
    add_header Access-Control-Allow-Origin *;
}
```

**Apache (.htaccess):**
```apache
<Files "version.json">
    Header set Access-Control-Allow-Origin "*"
</Files>
```

---

## 🔧 Шаг 3: Настройка Bootstrapper

### 3.1. Обновите конфигурацию

Откройте файл `bootstrapper/electron-bootstrapper/main.js` и измените URL:

```javascript
const CONFIG = {
  // URL к файлу version.json на вашем сервере
  versionUrl: 'https://ваш-домен.com/version.json',
  
  // URL для скачивания ZIP (можно не указывать, если есть в version.json)
  appDownloadUrl: 'https://ваш-домен.com/downloads/RobBob-App.zip',
  
  // Настройки локальных путей (обычно не нужно менять)
  appFolder: 'RobBob',
  appExecutable: 'RobBob.exe',
  versionFile: 'version.txt'
};
```

### 3.2. Соберите Bootstrapper

```bash
# Перейдите в папку bootstrapper
cd bootstrapper/electron-bootstrapper

# Установите зависимости
npm install

# Соберите установщик
npm run build
```

После сборки в папке `bootstrapper/electron-bootstrapper/dist/` появятся:
- `RobBob Launcher-Setup.exe` - установщик с NSIS
- `RobBob Launcher-Portable.exe` - портативная версия

---

## 📤 Шаг 4: Публикация

### 4.1. Разместите Bootstrapper на сайте

Загрузите файл `RobBob Launcher-Setup.exe` на ваш сайт, чтобы пользователи могли его скачать.

**Пример кнопки на сайте:**
```html
<a href="/downloads/RobBob-Launcher-Setup.exe" download>
  Скачать RobBob Launcher
</a>
```

### 4.2. Пользователь устанавливает

1. Пользователь скачивает маленький `RobBob Launcher-Setup.exe` (~10-20 МБ)
2. Запускает установщик
3. Bootstrapper автоматически:
   - Скачивает полную версию с вашего сервера
   - Устанавливает в `%LOCALAPPDATA%\RobBob\`
   - Создаёт ярлык на рабочем столе
   - Запускает лаунчер

---

## 🔄 Как работают обновления

### Автоматическое обновление

1. Пользователь запускает лаунчер (через ярлык на рабочем столе)
2. Bootstrapper проверяет версию на сервере
3. Если версия новее - автоматически скачивает и устанавливает обновление
4. Запускает обновлённую версию

### Публикация обновления

Чтобы опубликовать новую версию:

1. **Соберите новую версию основного лаунчера:**
   ```bash
   npm run build
   ```

2. **Создайте новый ZIP архив:**
   ```bash
   cd dist
   Rename-Item "RobBob-Portable.exe" "RobBob.exe"
   Compress-Archive -Path * -DestinationPath "../RobBob-App.zip" -Force
   ```

3. **Обновите version.json на сервере:**
   ```json
   {
     "version": "1.0.1",
     "downloadUrl": "https://ваш-домен.com/downloads/RobBob-App.zip"
   }
   ```

4. **Загрузите новый ZIP на сервер**

Всё! При следующем запуске пользователи автоматически получат обновление.

---

## 📂 Структура после установки

После установки у пользователя будут следующие файлы:

```
%LOCALAPPDATA%\
└── RobBob\
    ├── RobBob.exe           # Основное приложение
    ├── version.txt          # Текущая установленная версия
    ├── resources\           # Ресурсы приложения
    │   └── bypass\         # Файлы для оптимизации сети
    └── ... другие файлы

%APPDATA%\
└── robbob-launcher\        # Настройки пользователя
    └── config.json

Desktop\
└── RobBob Launcher.lnk    # Ярлык для запуска
```

---

## 🔍 Как это работает изнутри

### При первом запуске:

1. Пользователь запускает `RobBob Launcher-Setup.exe`
2. Bootstrapper открывает окно с прогрессом
3. Проверяет `version.json` на сервере
4. Скачивает `RobBob-App.zip` с прогрессом (0-100%)
5. Распаковывает в `%LOCALAPPDATA%\RobBob\`
6. Сохраняет версию в `version.txt`
7. Создаёт ярлык на рабочем столе
8. Запускает `RobBob.exe`
9. Закрывается

### При последующих запусках:

1. Пользователь кликает ярлык на рабочем столе
2. Запускается Bootstrapper (не основной лаунчер!)
3. Проверяет версию: локальную vs серверную
4. Если версия совпадает - сразу запускает `RobBob.exe`
5. Если версия новее - скачивает обновление, затем запускает
6. Закрывается

---

## ⚙️ Настройки для продакшна

### Рекомендуемые URL структуры

**Вариант 1: Статический сайт**
```
https://robbob-launcher.com/
├── version.json
└── downloads/
    └── RobBob-App.zip
```

**Вариант 2: CDN**
```
https://cdn.robbob-launcher.com/
├── version.json
└── RobBob-App.zip
```

**Вариант 3: GitHub Releases**
```json
{
  "version": "1.0.0",
  "downloadUrl": "https://github.com/user/repo/releases/latest/download/RobBob-App.zip"
}
```

### Тестирование

Перед публикацией проверьте:

```bash
# 1. Проверьте доступность version.json
curl https://ваш-домен.com/version.json

# 2. Проверьте доступность ZIP файла
curl -I https://ваш-домен.com/downloads/RobBob-App.zip

# 3. Запустите bootstrapper локально для теста
cd bootstrapper/electron-bootstrapper
npm start
```

---

## 🐛 Решение проблем

### Bootstrapper не может скачать файлы

**Проблема:** "Не удалось подключиться к серверу"

**Решение:**
1. Проверьте доступность URL в браузере
2. Проверьте CORS заголовки
3. Убедитесь, что нет переадресации (301/302)

### Не создаётся ярлык

**Проблема:** Ярлык не появляется на рабочем столе

**Решение:**
1. Проверьте права доступа к рабочему столу
2. Ярлык создаётся только при первой установке или обновлении
3. Проверьте логи в консоли разработчика

### ZIP файл не распаковывается

**Проблема:** "Ошибка при распаковке"

**Решение:**
1. Убедитесь, что ZIP файл не повреждён
2. Проверьте структуру: `RobBob.exe` должен быть в корне
3. Не используйте вложенные папки в ZIP

---

## 📊 Мониторинг

Вы можете отслеживать скачивания, добавив логирование на сервере:

**Nginx:**
```nginx
access_log /var/log/nginx/robbob-downloads.log combined;
```

**Анализ:**
```bash
# Количество скачиваний version.json (проверки обновлений)
grep "version.json" /var/log/nginx/robbob-downloads.log | wc -l

# Количество скачиваний ZIP (новые установки + обновления)
grep "RobBob-App.zip" /var/log/nginx/robbob-downloads.log | wc -l
```

---

## 🎉 Готово!

Теперь у вас настроена полная система автоматического обновления:

1. ✅ Маленький bootstrapper для скачивания пользователями
2. ✅ Автоматическое скачивание основного лаунчера
3. ✅ Автоматические обновления через сервер
4. ✅ Создание ярлыка на рабочем столе

**Для обновления лаунчера:**
1. Соберите новую версию
2. Обновите version.json
3. Загрузите новый ZIP на сервер

Пользователи автоматически получат обновление при следующем запуске!
