# Полная инструкция по установке и запуску RobBob Launcher

## Обзор

Система состоит из двух частей:
1. **Bootstrapper (Лаунчер)** - маленькая программа (~15MB), которую скачивают пользователи
2. **RobBob App** - основное приложение (~100MB), которое скачивается лаунчером

---

## Часть 1: Подготовка проекта

### Шаг 1.1: Установка Node.js

1. Скачайте Node.js с https://nodejs.org/ (версия LTS)
2. Установите, следуя инструкциям
3. Проверьте установку:
   ```bash
   node --version
   npm --version
   ```

### Шаг 1.2: Клонирование проекта

```bash
git clone https://github.com/notebookghoul-design/robobys.git
cd robobys
```

### Шаг 1.3: Установка зависимостей

```bash
npm install
```

---

## Часть 2: Настройка GitHub Releases (бесплатный хостинг)

### Шаг 2.1: Создание репозитория для файлов

1. Перейдите на https://github.com/new
2. Создайте новый репозиторий, например `robbob-releases`
3. Сделайте его **Public**

### Шаг 2.2: Настройка URL в коде

Откройте файл `bootstrapper/electron-bootstrapper/main.js` и измените:

```javascript
const CONFIG = {
  // Замените YOUR_USERNAME на ваш GitHub username
  // Замените robbob-releases на название вашего репозитория
  versionUrl: 'https://api.github.com/repos/YOUR_USERNAME/robbob-releases/releases/latest',
  appDownloadUrl: '',  // Будет браться из релиза
  appFolder: 'RobBob',
  appExecutable: 'RobBob.exe',
  versionFile: 'version.txt'
};
```

### Шаг 2.3: Обновление функции получения версии

В том же файле замените функцию `bootstrap()`:

```javascript
async function bootstrap() {
  try {
    sendStatus('status', 'Проверка обновлений...');
    
    let serverVersion = null;
    let downloadUrl = null;
    
    try {
      // Получаем последний релиз с GitHub
      const release = await fetchJson(CONFIG.versionUrl);
      
      if (release && release.tag_name) {
        serverVersion = release.tag_name.replace('v', '');
        
        // Ищем zip файл в assets
        const asset = release.assets.find(a => a.name.endsWith('.zip'));
        if (asset) {
          downloadUrl = asset.browser_download_url;
        }
      }
    } catch (e) {
      console.log('Failed to fetch release info:', e.message);
      if (isAppInstalled()) {
        runApp();
        return;
      }
      sendStatus('error', 'Не удалось подключиться к серверу');
      return;
    }
    
    if (!downloadUrl) {
      sendStatus('error', 'Файлы для скачивания не найдены');
      return;
    }
    
    const localVersion = getLocalVersion();
    const needsDownload = !isAppInstalled() || (serverVersion && serverVersion !== localVersion);
    
    if (needsDownload) {
      isDownloading = true;
      sendStatus('status', 'Загрузка обновления...', 0);
      
      const tempZip = path.join(app.getPath('temp'), 'robbob-update.zip');
      
      await downloadFile(downloadUrl, tempZip, (percent) => {
        sendStatus('status', `Загрузка: ${percent}%`, percent);
      });
      
      sendStatus('status', 'Распаковка файлов...', 100);
      await extractZip(tempZip, getAppDataPath());
      
      if (serverVersion) {
        saveLocalVersion(serverVersion);
      }
      
      isDownloading = false;
    }
    
    runApp();
    
  } catch (err) {
    console.error('Bootstrap error:', err);
    isDownloading = false;
    sendStatus('error', `Ошибка: ${err.message}`);
  }
}
```

---

## Часть 3: Сборка приложений

### Шаг 3.1: Сборка основного приложения (RobBob App)

```bash
# В корневой папке проекта
npm run build
```

После сборки в папке `dist/win-unpacked/` появятся файлы приложения.

### Шаг 3.2: Создание архива для загрузки

1. Откройте папку `dist/win-unpacked/`
2. Выделите ВСЕ файлы внутри
3. Правый клик -> "Отправить" -> "Сжатая ZIP-папка"
4. Назовите архив `RobBob-App.zip`

### Шаг 3.3: Сборка Bootstrapper (Лаунчера)

```bash
cd bootstrapper/electron-bootstrapper
npm install
npm run build
```

После сборки в папке `dist/` появится:
- `RobBob Launcher-Setup.exe` - установщик для пользователей

---

## Часть 4: Публикация на GitHub

### Шаг 4.1: Создание первого релиза

1. Перейдите в ваш репозиторий `robbob-releases`
2. Нажмите "Releases" справа
3. Нажмите "Create a new release"
4. Заполните:
   - **Tag version**: `v1.0.0`
   - **Release title**: `RobBob v1.0.0`
   - **Description**: Описание изменений
5. Прикрепите файл `RobBob-App.zip` (drag & drop или кнопка "Attach files")
6. Нажмите "Publish release"

### Шаг 4.2: Распространение лаунчера

Файл `RobBob Launcher-Setup.exe` - это то, что вы даете пользователям.
Его можно:
- Загрузить на Google Drive / Яндекс.Диск
- Прикрепить к посту в Discord
- Выложить на свой сайт

---

## Часть 5: Обновление приложения

Когда вы хотите выпустить обновление:

### Шаг 5.1: Внесите изменения в код

### Шаг 5.2: Обновите версию

Откройте `package.json` и измените версию:
```json
{
  "version": "1.1.0"
}
```

### Шаг 5.3: Соберите новую версию

```bash
npm run build
```

### Шаг 5.4: Создайте новый zip архив

Заархивируйте содержимое `dist/win-unpacked/` в `RobBob-App.zip`

### Шаг 5.5: Создайте новый релиз на GitHub

1. Перейдите в Releases
2. "Create a new release"
3. Tag: `v1.1.0`
4. Прикрепите новый `RobBob-App.zip`
5. Publish

**Готово!** При следующем запуске лаунчера пользователи автоматически получат обновление.

---

## Часть 6: Настройка новостей

### Шаг 6.1: Создание GitHub Gist для новостей

1. Перейдите на https://gist.github.com
2. Создайте новый Gist:
   - Filename: `news.json`
   - Содержимое:
   ```json
   {
     "news": [
       {
         "id": "1",
         "title": "Запуск RobBob!",
         "content": "Добро пожаловать в RobBob Launcher!",
         "date": "2024-12-13",
         "emoji": "🎉",
         "pinned": true
       }
     ]
   }
   ```
3. Нажмите "Create public gist"
4. Нажмите кнопку "Raw" и скопируйте URL

### Шаг 6.2: Настройка URL в приложении

Откройте `src/scripts/news.js` и замените URL:
```javascript
const NEWS_URL = 'https://gist.githubusercontent.com/YOUR_USERNAME/GIST_ID/raw/news.json';
```

---

## Структура файлов на GitHub Releases

```
Ваш репозиторий robbob-releases:
└── Releases/
    ├── v1.0.0/
    │   └── RobBob-App.zip (прикреплен к релизу)
    ├── v1.1.0/
    │   └── RobBob-App.zip
    └── ...
```

---

## Troubleshooting

### Лаунчер не видит обновления
- Проверьте что релиз опубликован (не Draft)
- Проверьте что zip файл прикреплен к релизу
- Проверьте URL в CONFIG

### Приложение не запускается после скачивания
- Убедитесь что в архиве есть `RobBob.exe` (имя должно совпадать с CONFIG.appExecutable)
- Проверьте структуру архива - файлы должны быть в корне, не в подпапке

### Bypass/Zapret не работает
- Запустите лаунчер от имени администратора
- Проверьте что все файлы есть в `resources/bypass/`
- Проверьте антивирус - он может блокировать winws.exe

---

## Контакты

PR с кодом: https://github.com/notebookghoul-design/robobys/pull/1
