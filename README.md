# RobBob Launcher

Десктопное приложение для быстрого доступа к Roblox со встроенным обходом блокировок.

## 🚀 Возможности

- **Быстрый запуск Roblox** - одним кликом
- **Встроенный обход блокировок** - работает автоматически при запуске
- **Тёмная/светлая тема** - с плавной анимацией перехода
- **Сворачивание в трей** - работает в фоне
- **Статус серверов** - мониторинг пинга

## 📦 Установка

### Из исходников

1. Установите [Node.js](https://nodejs.org/) (версия 18+)

2. Клонируйте репозиторий:
```bash
git clone https://github.com/your-repo/robbob-launcher.git
cd robbob-launcher
```

3. Установите зависимости:
```bash
npm install
```

4. **Добавьте файлы обхода:**
   - Скачайте [zapret-discord-youtube](https://github.com/Flowseal/zapret-discord-youtube/releases)
   - Распакуйте содержимое в папку `resources/bypass/`
   - Убедитесь, что там есть файлы `.bat` и `winws.exe`

5. Запустите в режиме разработки:
```bash
npm start
```

### Сборка

```bash
# Для Windows
npm run build:win

# Для macOS
npm run build:mac

# Для Linux
npm run build:linux
```

Собранные файлы будут в папке `dist/`.

## 🛡️ Обход блокировок

Обход блокировок **включён по умолчанию** и запускается автоматически вместе с лаунчером. Вы можете отключить его в настройках.

Обход работает локально на вашем компьютере и не передаёт данные на сторонние серверы.

## 📁 Структура проекта

```
robbob-launcher/
├── main.js              # Главный процесс Electron
├── preload.js           # Preload скрипт (мост между Node.js и браузером)
├── package.json         # Зависимости и конфигурация
├── resources/
│   └── bypass/          # Файлы обхода блокировок
├── src/
│   ├── index.html       # Главная страница
│   ├── styles/
│   │   └── main.css     # Стили приложения
│   └── scripts/
│       ├── app.js       # Основная логика
│       ├── theme.js     # Управление темой
│       ├── navigation.js# Навигация
│       ├── modals.js    # Модальные окна
│       └── bypass.js    # Управление обходом
└── assets/
    ├── icon.png         # Иконка приложения
    ├── icon.ico         # Иконка для Windows
    └── background.jpg   # Фоновое изображение
```

## ⚙️ Конфигурация

Настройки хранятся в:
- Windows: `%APPDATA%\robbob-launcher\config.json`
- macOS: `~/Library/Application Support/robbob-launcher/config.json`
- Linux: `~/.config/robbob-launcher/config.json`

## 🔧 Разработка

### Режим разработки с DevTools
```bash
npm start -- --dev
```

### Горячая перезагрузка
Для разработки рекомендуется использовать:
```bash
npm install -D electron-reload
```

## 📝 Лицензия

MIT License

## ⚠️ Отказ от ответственности

Это приложение предоставляется "как есть". Авторы не несут ответственности за возможные последствия использования.
